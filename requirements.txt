datasets>=2.18
openai>=1.30
sentence-transformers>=2.7
pandas>=2.0
python-dotenv>=1.0
tenacity>=8.2
matplotlib>=3.0
pydantic>=1.10
PyYAML>=6.0
faiss-cpu>=1.8
streamlit>=1.28
plotly>=5.13
# Real toxicity classifier for the toxicity benchmark (downloads model weights
# on first use). Installed by default; the benchmark FAILS FAST if it is absent
# rather than silently scoring with a weak lexical scanner. Strip it only if you
# never run `bench toxicity` (or always pass --toxicity-backend lexical).
detoxify>=0.5
